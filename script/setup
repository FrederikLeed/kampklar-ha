#!/usr/bin/env bash
# Setup development environment for KampKlar HA
set -euo pipefail

cd "$(dirname "$0")/.."

echo "Installing dev dependencies..."
pip install -r requirements_dev.txt

echo "Installing test dependencies..."
pip install -r requirements_test.txt

echo "Done! Run 'script/develop' to start Home Assistant."
